import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.llbnew.newtestbybobll',
  appName: 'new-test-by-bob-2',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
